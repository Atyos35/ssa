doctrine:
    dbal:
        # Configuration spécifique aux tests
        driver: pdo_pgsql
        server_version: '16'
        charset: utf8
        options:
            PDO::ATTR_ERRMODE: PDO::ERRMODE_EXCEPTION
