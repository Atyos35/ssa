monolog:
    channels: ['messenger']
    handlers:
        main:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
            channels: ["!event", "!messenger"]
        # uncomment to get logging in your browser
        # you may have to allow bigger header sizes in your Web server configuration
        #firephp:
        #    type: firephp
        #    level: info
        #chromephp:
        #    type: chromephp
        #    level: info
        console:
            type: console
            process_psr_3_messages: false
            channels: ["!event", "!doctrine", "!messenger"]
        messenger:
            type: stream
            path: "%kernel.logs_dir%/messenger.log"
            level: debug
            channels: ["messenger"]
        # Uncomment when you want to see all messages in the logs
        # messenger_failed:
        #     type: stream
        #     path: "%kernel.logs_dir%/messenger_failed.log"
        #     level: !php/const Monolog\Logger::DEBUG
        #     channels: ["messenger"] 